interface ITheme {
  color: {
    blue: string;
    gray: string;
    darkGray: string;
    borderColor: string;
    black: string;
    white: string;
  };
}

export const theme = {
  color: {
    blue: "#1DA1F2",
    gray: "#8899A6",
    darkGray: "#253343",
    borderColor: "#2F3336",
    black: "#000",
    white: "#fff",
  },
};
